{
  "name": "@inclulearn-ai/newsroom",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "content:check": "node scripts/validate-content.mjs",
    "build": "npm run content:check && astro check && astro build && pagefind --site dist && node scripts/validate-search-build.mjs",
    "preview": "astro preview",
    "check": "npm run content:check && astro check"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.0",
    "astro": "^7.3.3",
    "typescript": "^5.9.0"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
